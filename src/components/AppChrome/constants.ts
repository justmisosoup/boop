/**
 * Layout constants for the app chrome.
 *
 * The rail widths are also expressed as Tailwind utilities inside
 * `AppShellSidebar` (`w-[224px]` / `w-[49px]`); they must stay in sync with
 * these values, which the content offset (`--nav-w` on the shell root)
 * consumes.
 */
export const COLLAPSED_WIDTH = '49px'
export const EXPANDED_WIDTH = '224px'
